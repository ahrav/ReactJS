import React from 'react'

const header = props => {
    return (
        <header id="header" className="alt">
        <nav>
          <a href="#menu">Menu</a>
        </nav>
      </header>
    )
}

export default header